name: Cloudflare Workers

on:
  workflow_dispatch:
    inputs:
      deploy:
        description: Deploy workers after validation
        required: true
        default: true
        type: boolean
  push:
    branches:
      - main
    paths:
      - 'cloudflare/**'
      - '.github/workflows/cloudflare-workers.yml'

jobs:
  validate-match-relay:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: cloudflare/match-relay
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: npm install
      - name: Bundle and validate match relay
        run: npm run check

  deploy-match-relay:
    if: ${{ github.event_name == 'push' || inputs.deploy }}
    needs: [validate-match-relay]
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: cloudflare/match-relay
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Require Cloudflare secrets
        shell: bash
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
        run: |
          test -n "$CLOUDFLARE_API_TOKEN" || { echo "::error::CLOUDFLARE_API_TOKEN is not configured"; exit 1; }
          test -n "$CLOUDFLARE_ACCOUNT_ID" || { echo "::error::CLOUDFLARE_ACCOUNT_ID is not configured"; exit 1; }
      - run: npm install
      - name: Deploy match relay
        run: npx wrangler deploy
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}

  validate-voice-worker:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: cloudflare/voice-worker
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: npm install
      - name: Bundle and validate voice worker
        run: npm run check

  deploy-voice-worker:
    if: ${{ github.event_name == 'workflow_dispatch' && inputs.deploy }}
    needs: [validate-voice-worker]
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: cloudflare/voice-worker
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Require voice deployment secrets
        shell: bash
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          REALTIME_SFU_APP_ID: ${{ secrets.REALTIME_SFU_APP_ID }}
          REALTIME_SFU_APP_SECRET: ${{ secrets.REALTIME_SFU_APP_SECRET }}
        run: |
          test -n "$CLOUDFLARE_API_TOKEN" || { echo "::error::CLOUDFLARE_API_TOKEN is not configured"; exit 1; }
          test -n "$CLOUDFLARE_ACCOUNT_ID" || { echo "::error::CLOUDFLARE_ACCOUNT_ID is not configured"; exit 1; }
          test -n "$REALTIME_SFU_APP_ID" || { echo "::error::REALTIME_SFU_APP_ID is not configured"; exit 1; }
          test -n "$REALTIME_SFU_APP_SECRET" || { echo "::error::REALTIME_SFU_APP_SECRET is not configured"; exit 1; }
      - run: npm install
      - name: Configure Realtime SFU secrets
        shell: bash
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          REALTIME_SFU_APP_ID: ${{ secrets.REALTIME_SFU_APP_ID }}
          REALTIME_SFU_APP_SECRET: ${{ secrets.REALTIME_SFU_APP_SECRET }}
        run: |
          printf '%s' "$REALTIME_SFU_APP_ID" | npx wrangler secret put REALTIME_SFU_APP_ID
          printf '%s' "$REALTIME_SFU_APP_SECRET" | npx wrangler secret put REALTIME_SFU_APP_SECRET
      - name: Deploy voice worker
        run: npx wrangler deploy
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
