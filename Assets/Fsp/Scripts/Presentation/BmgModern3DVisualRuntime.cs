using UnityEngine;

namespace Fsp.Presentation
{
    /// <summary>
    /// Intentionally disabled. Production BMG visuals must come from authored 3D assets only.
    /// Runtime primitive geometry is prohibited because it creates the blocky/black mixed style.
    /// </summary>
    public sealed class BmgModern3DVisualRuntime : MonoBehaviour
    {
        // No bootstrap on purpose.
    }
}
