{
  "name": "fsp-match-relay",
  "private": true,
  "scripts": {
    "check": "wrangler deploy --dry-run --outdir dist",
    "types": "wrangler types --check",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
